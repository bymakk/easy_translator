# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules/

# ── Служебный каталог разработки (не публикуется) ────────────────────────────
backend/

# ── Build artefacts (local only) ──────────────────────────────────────────────
dist/

# ── Source code — not published ───────────────────────────────────────────────
src/
scripts/
assets/
index.html
vite.config.js
vite.extension.config.js
version-state.json
package.json
package-lock.json

# ── chrome-extension source (build/ folder IS committed) ──────────────────────
chrome-extension/manifest.json
chrome-extension/options.html

# ── Secrets / keys ────────────────────────────────────────────────────────────
.env
ssh-key*/
*.pem
id_rsa*
id_ed25519*
*.key

# ── OS / tooling ──────────────────────────────────────────────────────────────
.DS_Store
*.local
.playwright-mcp/
bundle-stats.html

# ── Landing page (deployed separately to easytranslator.ru) ───────────────────
web/

# ── Screenshots / prototyping (корневые картинки не в репо) ───────────────────
/*.png
/*.jpg
/*.jpeg
